require 'spec_helper'

describe "beta_tests/show.html.erb" do
  before(:each) do
    @beta_test = assign(:beta_test, stub_model(BetaTest))
  end

  it "renders attributes in <p>" do
#    render
  end
end
