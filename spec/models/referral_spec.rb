require 'spec_helper'

describe Referral do
  it "finds a reference id through the email address"
  describe "validations" do
    let(:model) { Factory.build :referral }
    it "has a valid email"
  end
end
