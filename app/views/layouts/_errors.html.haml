-if object.errors.any?
  .bubble-top-error
  .bubble-middle-error.align-left
    %h3= "#{pluralize(object.errors.count, "error")} prevented this from being saved:"
    %ul
      -object.errors.full_messages.each do |msg|
        %li= msg
  .bubble-bottom-error
