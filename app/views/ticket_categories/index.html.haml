%h1 Listing ticket_categories

%table
  %tr
    %th
    %th
    %th

  - @ticket_categories.each do |ticket_category|
    %tr
      %td= link_to 'Show', ticket_category
      %td= link_to 'Edit', edit_ticket_category_path(ticket_category)
      %td= link_to 'Destroy', ticket_category, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Ticket category', new_ticket_category_path
