<%= form_for(@survey_option) do |f| %>
  <% if @survey_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_option.errors.count, "error") %> prohibited this survey_option from being saved:</h2>

      <ul>
      <% @survey_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
