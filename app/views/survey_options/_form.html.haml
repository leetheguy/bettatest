= form_for @survey_option do |f|
  -if @survey_option.errors.any?
    #error_explanation
      %h2= "#{pluralize(@survey_option.errors.count, "error")} prohibited this survey_option from being saved:"
      %ul
        - @survey_option.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
