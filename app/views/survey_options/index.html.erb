<h1>Listing survey_options</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @survey_options.each do |survey_option| %>
  <tr>
    <td><%= link_to 'Show', survey_option %></td>
    <td><%= link_to 'Edit', edit_survey_option_path(survey_option) %></td>
    <td><%= link_to 'Destroy', survey_option, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Survey option', new_survey_option_path %>
